export const createSuccesfull = (status:string, message:string,data?:any)=>{
    return {status,message,data}
}
export const loginSuccessfull = (status:string, message:string,data?:any)=>{
    return {status,message,data}
}